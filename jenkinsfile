pipeline {
    agent { 
                label 'test-node'
            }
    stages {
        stage('Example Build') {
            steps {

                sh ''' 
                ls
                
                pwd
                docker run -d -p 80:80 nginx
date
                '''
            }
        }
    }
}