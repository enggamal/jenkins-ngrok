pipeline {
    agent { 
                label 'test-node'
            }
    stages {
        stage('Example Build') {
            steps {

                sh ''' 
                whoami
                pwd
                docker run -d -p 80:80 nginx

                '''
            }
        }
    }
}